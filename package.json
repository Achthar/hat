{
  "name": "hat-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "HAT - Human Attention Token: Verified human attention marketplace on Arc",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "zip:extension": "node scripts/zip-extension.mjs",
    "deploy:web": "pnpm --filter @hat/web build && wrangler pages deploy packages/web/dist --project-name=hat",
    "deploy:advertiser": "pnpm --filter @hat/advertiser build && wrangler pages deploy packages/advertiser/dist --project-name=hat-advertiser",
    "deploy": "pnpm deploy:web && pnpm deploy:advertiser"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "sharp",
      "workerd"
    ]
  }
}
