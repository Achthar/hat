{
  "name": "@hat/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url $ARC_RPC_URL --broadcast --verify",
    "clean": "forge clean"
  }
}
